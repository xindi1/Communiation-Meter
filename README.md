# Communication Meter v1.1 Data Module Update

Updates:
- Mobile-safe Data panel buttons: Export / Import / Clear All stack on small screens.
- Export stores app/version/timestamps/timezone and updates Last export.
- Import merges by id instead of replacing local data.
- Invalid JSON shows an error toast.
- Service worker cache bumped.
